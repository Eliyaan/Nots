Test more junctions -> runeed the auto placer

save system


very bad perf when lot of the screen covered by drawing -> replace only what needs to be replaced ?

gates

buttons (keyboard + click)
levers because nice


// Rules to remember:
Not state updated before queuing them
Junctions not in wire outputs
Junctions not queued just loop till other side